{
  "name": "media-free-browser",
  "version": "1.0.0",
  "private": true,
  "description": "A proxy browser that loads pages without images, video, or audio.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.21.2"
  }
}
