services:
  - type: web
    name: quiet-browser
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      # Set this in the Render dashboard when prompted. Without it, anyone who
      # finds the URL can browse through your instance.
      - key: ACCESS_PASSWORD
        sync: false
      - key: SESSION_SECRET
        generateValue: true
